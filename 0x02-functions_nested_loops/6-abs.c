#include "main.h"

/**
 * _abs - computes the absoukt value of an int
 * @Ava: int to check
 * Return: return value of _abs
 */

int _abs(int Ava)
{
	if (Ava >= 0)
	{
		return (Ava);
	}

	return (-Ava);
}
