#include "main.h"

/**
 * _isdigit - checks if an entry is a digit for.digits 0-9
 * @a: value to check
 * Return: returns 0 if entry isnt a digit or 1 if entry is a digit"
 */

int _isdigit(int a)
{
	return (a >= '0' && a <= '9');
}

