#include "main.h"

/**
 * mul - multipiles 2 integers
 * @a: integer to multiply
 * @b: integer to multiply by
 * Return: 0 if successful
 */

int mul(int a, int b)
{
	int c;

	c = a * b;

	return (c);
}
